// import axios from 'axios'

// const instance = axios.create({
//   baseURL: 'http://192.168.1.8:3050', // Base URL for your backend API
//   timeout: 10000 // Optional: Set a timeout for requests (in milliseconds)
//   // headers: {
//   //   'Content-Type': 'application/json' // Default headers
//   // }
// })

// // Add interceptors for request or response if needed
// instance.interceptors.request.use(    "react-hot-toast": "^2.4.1"
//   (config) => {
//     // Modify the request before sending it
//     return config
//   },
//   (error) => Promise.reject(error)
// )

// instance.interceptors.response.use(
//   (response) => response,
//   (error) => Promise.reject(error)
// )

// export default instance // Corrected: Export the instance properly
